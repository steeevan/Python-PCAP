# Explain the difference between mutable and immutable types in Python. Provide examples of each.

# Mutable types can have their elements manipulated, while immutable types aren't able to.

mutable = [1,2,3,4,5]

immutable = (1,2,3,4,5)