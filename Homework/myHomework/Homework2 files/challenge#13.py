# 13:
def gcd(num1,num2):
    import math
    return math.gcd(num1,num2)

print(gcd(10,20))

# This program works by using the math module. It uses the math.gcd() function to calculate the gcd of the two numbers given
# and returns this value.