# 12:
def circ_area(radius):
    import math
    return math.pi * math.pow(radius,2)
print(circ_area(1))

# This program works by using the math module to get pi. It then takes the radius, squares it, and multplies it by pi. Then,
# it returns this value.