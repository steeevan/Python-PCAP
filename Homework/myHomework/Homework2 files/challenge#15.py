# 15:
def expower(n):
    import math
    return math.exp(n)
print(expower(10))

# This program works by accepting 1 argument, number whose power you want to calculate. It then uses the math module's exp()
# function, inputs the number in it, and returns the value.